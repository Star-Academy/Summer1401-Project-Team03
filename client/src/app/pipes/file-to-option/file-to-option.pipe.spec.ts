import { FileToOptionPipe } from './file-to-option.pipe';

describe('FileToOptionPipe', () => {
  it('create an instance', () => {
    const pipe = new FileToOptionPipe();
    expect(pipe).toBeTruthy();
  });
});
